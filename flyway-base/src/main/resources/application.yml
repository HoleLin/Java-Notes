server:
  port: 8092

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/flyway-test?useUnicode=true&characterEncoding=utf-8&serverTimezone=UTC&useSSL=false
    username: root
    password: holelin..
    driver-class-name: com.mysql.cj.jdbc.Driver
  flyway:
    enabled: true
    baseline-on-migrate: true
    sqlMigrationPrefix: BASE_